---
"@bnidev/js-utils": patch
---

fix(storage): re-export `getCookie`, `setCookie`, `getStorage`, and `setStorage` from the package root so they are importable, included in the build, and covered by generated docs
